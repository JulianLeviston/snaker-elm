{
  "name": "snaker-assets",
  "version": "0.1.0",
  "description": "Snaker frontend assets",
  "repository": {},
  "license": "MIT",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "deploy": "NODE_ENV=production node build.js"
  },
  "dependencies": {
    "phoenix": "file:../deps/phoenix",
    "phoenix_html": "file:../deps/phoenix_html"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "esbuild-plugin-elm": "^0.0.12",
    "typescript": "^5.3.0"
  }
}
